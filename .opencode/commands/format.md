---
description: Format modified files using ruff / black for Python and Prettier for TypeScript / React / JSON
agent: codi
---

Format the specified file or workspace using production formatters:
Formatting target: $ARGUMENTS

Ensure code styling conforms to standard indentation, import sorting, and trailing comma rules without altering AST logic.
